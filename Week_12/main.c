#include "./Recursion_Example.h"

int main(){
    // Let's call some functions!
    // First, factorial
    printf("5! = %d\n", factorial(5));
    printf("10! = %d\n", factorial(10));
    printf("-1! will give us %d\n", factorial(-1));

    return 0;

}